'use strict';

(function () {
  document.addEventListener('DOMContentLoaded', () => {
    if (!window.MinimaxAI) {
      console.error('MinimaxAI module failed to initialise before game.js.');
    }
  });
})();
